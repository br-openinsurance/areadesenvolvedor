{
  "_comment": "Override do estágio de versões específicas. Quando uma versão muda de estágio (ex: certifying→current, current→deprecated), adicione ou atualize a entrada abaixo. O sync_apis_json.py usará este estágio no lugar do estágio da pasta física, sem mover nenhum arquivo. Estágios válidos: certifying, current, deprecated, retired.",
  "overrides": {
  }
}
